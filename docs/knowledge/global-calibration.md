# 全プロジェクト横断 工数温度感（global）

> このファイルは `/sf-memory` cat6-global（sf-analyst-cat6-global）が自動生成します。
> 手動編集は最小限にしてください（次回生成時に「全体傾向」セクションが上書きされます）。

生成日: （未生成）
データソース: 全プロジェクト Backlog 完了課題（actualHours 記録あり）

## 全体傾向
- （cat6-global 実行後に自動生成されます）

## コンポーネント種別別の工数帯

（cat6-global 実行後に自動生成されます）
