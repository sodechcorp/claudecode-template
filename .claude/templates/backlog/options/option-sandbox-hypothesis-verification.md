# option-sandbox-hypothesis-verification

## 何をするか

Phase 1 で立てた全仮説を Sandbox で実際に操作して検証し、「再現する仮説」と「再現しない仮説」を分類する。
対応方針策定（Phase 2）は再現した仮説のみを対象とする。

## 前提

- `investigation.md` の「根本原因」セクションに仮説リストと再現条件が記載されていること
- `option-multi-cause-hypothesis` の結果が `investigation.md` に反映済みであること
- Sandbox 環境（接続済み `{sf_alias}`）に操作できること

仮説が 1 件しかない場合でも、自明なバグ（typo・null チェック漏れ等）以外は Sandbox での実証を省略しない。

## 実行手順

1. `investigation.md` の「根本原因」セクションから仮説リスト（H1〜Hn）と再現条件を抽出する
   - 各仮説の「前提データ・操作ユーザー・操作手順・期待結果」を確認する

2. Sandbox で各仮説を順次検証する（仮説ごとに独立した状態で実施すること）:
   a. 前提データを準備する（必要なレコードを作成、または本番コピーデータを利用）
   b. 指定操作ユーザーでログインする（代替ユーザーで代用する場合はその旨を記録）
   c. 再現手順を 1 ステップずつ実施する
   d. 期待結果と実測結果を記録する
   e. SOQL クエリ・Debug ログ・画面状態等の証跡を取得する（可能な範囲で）

3. 検証結果を分類する:
   - 再現した → ✅ 対応方針策定対象
   - 再現しなかった → ❌ 除外（記録のみ）
   - 検証不可（データ準備困難・環境依存等）→ ⚠️ 「要ユーザー対応」として記録

4. 結果に応じて分岐する:
   - 再現仮説 ≥ 1 件 → `hypothesis-verification.md` を保存して完了
   - 再現仮説 = 0 件 → 「全仮説が再現せず」と記録し、`investigation.md` の仮説立て直しを提案して Phase 1 に戻す
   - 検証中に新事実発見 → `investigation.md` の仮説・再現条件を更新してから Phase 1.6 を再実施

## 出力

`docs/logs/{issueID}/hypothesis-verification.md` に以下の形式で保存する:

```markdown
# Phase 1.6 Sandbox 仮説検証結果

作成日時: {YYYY-MM-DD HH:MM}

## 対象環境
- Sandbox エイリアス: {sf_alias}
- 本番再現データ: {record_id}（コピー or 模擬）

## 検証対象仮説（investigation.md より）
| # | 仮説 | 採用尤度（事前） |
|---|---|---|
| H1 | ... | 高 |
| H2 | ... | 中 |
| H3 | ... | 中 |

## 検証手順と結果

### H1: {仮説名}
**事前条件**: ...
**実行操作**: ...
**期待結果**: 症状が再現する
**実測結果**: 再現した / 再現しなかった
**証跡**: スクリーンショット / ログ / SOQL クエリ結果

### H2: {仮説名}
...

## 検証サマリー

| # | 仮説 | 検証結果 | 採用判定 |
|---|---|---|---|
| H1 | ... | 再現 | ✅ 対応方針策定対象 |
| H2 | ... | 再現せず | ❌ 除外（記録のみ） |
| H3 | ... | 再現 | ✅ 対応方針策定対象 |

## 結論
- 採用候補仮説: H1, H3（2 件）
- 除外仮説: H2（再現せず）
- 次フェーズ: Phase 2 で H1 と H3 の対応方針を策定する
```
